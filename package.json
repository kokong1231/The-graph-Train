{
  "name": "vending_",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ vending_",
    "create-local": "graph create --node http://localhost:8020/ vending_",
    "remove-local": "graph remove --node http://localhost:8020/ vending_",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vending_"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
